# Tax Pay Calculator

This is a simple .NET console application that calculates tax owed based on taxable income. The program reads tax brackets from a CSV file, applies the appropriate tax rules, and outputs the calculated tax while handling invalid input using exception handling.

